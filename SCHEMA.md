# UniProtKB Parquet lake — schema 1.0.0

Generated by `bin/gen_schema_md.py` from a `2026_01` build (manifest format version 2). Column names, types and order are frozen at this schema version; see the policy at the end.

## Tables

| Table | Rows in this build | Description |
| --- | --- | --- |
| `entries` | 5,378 | One row per protein. Primary table for most queries. |
| `features` | 10,817 | One row per positional annotation (domain, signal, transmembrane, etc). Sorted by (reviewed DESC, taxid ASC, acc ASC) for locality; use ORDER BY start_pos for position-sorted queries within a protein. |
| `xrefs` | 95,385 | One row per cross-reference to an external database (PDB, Ensembl, GO, InterPro, etc). |
| `comments` | 4,261 | One row per comment annotation (function, subcellular location, disease, etc). |
| `publications` | 18,175 | One row per literature citation or submission (derived from UniProtKB entry.references). |
| `accession_map` | 5,740 | One row per primary or secondary accession, mapping to the current primary accession. The lookup table for point queries and for resolving retired accessions. |

## `entries`

One row per protein. Primary table for most queries.

- Primary key: `acc`
- Sort order: `reviewed DESC, taxid ASC, acc ASC`
- Partitioning: hive by `review_status=swissprot|trembl`

| Column | Type | Category | Source path | Description |
| --- | --- | --- | --- | --- |
| `acc` | `string` | convenience | `primaryAccession` | UniProtKB primary accession (e.g. P05067). Stable identifier; unique per entry. |
| `id` | `string` | convenience | `uniProtkbId` | UniProtKB mnemonic entry name (e.g. A4_HUMAN). May change between releases. |
| `reviewed` | `bool` | convenience | `entryType` | True for Swiss-Prot (manually reviewed); false for TrEMBL (automated annotation). |
| `taxid` | `int64` | convenience | `organism.taxonId` | NCBI taxonomy ID of the source organism (e.g. 9606 for Homo sapiens). |
| `organism_name` | `string` | convenience | `organism.scientificName` | Scientific name of the source organism. |
| `gene_names` | `list<l: string>` | convenience | `genes.geneName.value` | List of primary gene names across all genes associated with this entry. |
| `protein_name` | `string` | convenience | `proteinDescription.recommendedName.fullName.value` | Recommended (Swiss-Prot) or submitted (TrEMBL) full protein name. |
| `seq_length` | `int32` | convenience | `sequence.length` | Sequence length in amino acids. |
| `sequence` | `string` | convenience | `sequence.value` | Full amino acid sequence (one-letter IUPAC codes). |
| `gene_name` | `string` | convenience | derived | Primary gene name (first of gene_names). |
| `secondary_accs` | `list<l: string>` | convenience | `secondaryAccessions` | List of secondary (merged/demerged) accessions that resolve to this entry. |
| `organism_common` | `string` | convenience | `organism.commonName` | Common name of the source organism (e.g. 'Human'). May be null. |
| `lineage` | `list<l: string>` | convenience | `organism.lineage` | Full taxonomic lineage as a list of taxon names, root to species. |
| `division` | `string` | convenience | derived | UniProt taxonomic division (archaea, bacteria, fungi, human, invertebrates, mammals, plants, rodents, vertebrates, viruses, unclassified), derived from lineage and taxid. |
| `gene_synonyms` | `list<l: string>` | convenience | `genes.synonyms.value` | Flattened list of gene name synonyms across all genes. |
| `alt_protein_names` | `list<l: string>` | convenience | `proteinDescription.alternativeNames.fullName.value` | List of alternative full protein names from proteinDescription.alternativeNames. |
| `protein_flag` | `string` | convenience | `proteinDescription.flag` | Protein description flag: 'Precursor', 'Fragment', 'Precursor; Fragment', or null. |
| `ec_numbers` | `list<l: string>` | convenience | derived | Distinct EC (Enzyme Commission) numbers extracted from all naming blocks. |
| `protein_existence` | `string` | convenience | `proteinExistence` | Protein existence evidence level (e.g. 'Evidence at protein level'). |
| `annotation_score` | `double` | convenience | `annotationScore` | UniProt annotation score (1-5). Higher = more richly annotated. |
| `seq_mass` | `int32` | convenience | `sequence.molWeight` | Molecular weight of the unprocessed precursor in Daltons. |
| `seq_md5` | `string` | convenience | `sequence.md5` | MD5 hash of the sequence (lowercase hex). Useful for deduplication. |
| `seq_crc64` | `string` | convenience | `sequence.crc64` | CRC64 checksum of the sequence. Used by UniProt for integrity checks. |
| `go_ids` | `list<l: string>` | convenience | `uniProtKBCrossReferences.id` | Distinct Gene Ontology term IDs (e.g. GO:0005634) from cross-references. |
| `go_terms` | `STRUCT(id VARCHAR, aspect VARCHAR, term VARCHAR, evidence_type VARCHAR)[]` | convenience | derived | GO annotations as {id, aspect (P/F/C), term, evidence_type}; go_ids is the flat id list. |
| `xref_dbs` | `list<l: string>` | convenience | `uniProtKBCrossReferences.database` | Distinct database names referenced in cross-references (e.g. PDB, Ensembl). |
| `proteome_ids` | `list<l: string>` | convenience | `uniProtKBCrossReferences.id` | Distinct UniProt proteome ids (UP…) from cross-references, sorted. |
| `keyword_ids` | `list<l: string>` | convenience | `keywords.id` | UniProt keyword IDs (e.g. KW-0181). |
| `keyword_names` | `list<l: string>` | convenience | `keywords.name` | UniProt keyword names (e.g. 'Complete proteome'). |
| `keyword_categories` | `list<l: string>` | convenience | `keywords.category` | Keyword categories aligned with keyword_ids (e.g. Molecular function, Biological process). |
| `first_public` | `date32[day]` | convenience | `entryAudit.firstPublicDate` | Date the entry was first made public in UniProtKB. |
| `last_modified` | `date32[day]` | convenience | `entryAudit.lastAnnotationUpdateDate` | Date of the last annotation update. |
| `last_seq_modified` | `date32[day]` | convenience | `entryAudit.lastSequenceUpdateDate` | Date of the last sequence update. |
| `entry_version` | `int32` | convenience | `entryAudit.entryVersion` | Entry version number (incremented on any change). |
| `seq_version` | `int32` | convenience | `entryAudit.sequenceVersion` | Sequence version number (incremented only on sequence changes). |
| `feature_count` | `int32` | convenience | derived | Number of positional features (domains, sites, variants, etc.). |
| `xref_count` | `int32` | convenience | derived | Number of cross-references to external databases. |
| `comment_count` | `int32` | convenience | derived | Number of comment annotations (function, disease, etc.). |
| `reference_count` | `int32` | convenience | derived | Number of literature/submission references. |
| `pubmed_ids` | `list<l: string>` | convenience | derived | Distinct PubMed ids cited by the entry, sorted numerically, stored as strings. |
| `uniparc_id` | `string` | convenience | `extraAttributes.uniParcId` | UniParc identifier (UPI) linking to the sequence archive. |
| `entry_type` | `string` | convenience | `entryType` | Raw entryType string (e.g. 'UniProtKB reviewed (Swiss-Prot)'). Lossless. |
| `extra_attributes` | `STRUCT(countByCommentType MAP(VARCHAR, BIGINT), countByFeatureType MAP(VARCHAR, BIGINT), uniParcId VARCHAR)` | convenience | `extraAttributes` | Nested struct with countByCommentType, countByFeatureType, uniParcId. |
| `organism_residual` | `STRUCT(evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[], synonyms VARCHAR[])` | nested | `organism` | Organism fields not in the convenience columns (synonyms, evidences); merge with taxid, organism_name, organism_common, lineage to rebuild organism. |
| `protein_desc_residual` | `STRUCT(allergenName STRUCT("value" VARCHAR, evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[]), alternativeNames STRUCT(fullName STRUCT("value" VARCHAR, evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[]), shortNames STRUCT("value" VARCHAR, evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[])[], ecNumbers STRUCT(evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[], "value" VARCHAR)[])[], cdAntigenNames STRUCT("value" VARCHAR, evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[])[], contains STRUCT(recommendedName STRUCT(fullName STRUCT("value" VARCHAR, evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[]), shortNames STRUCT("value" VARCHAR, evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[])[], ecNumbers STRUCT(evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[], "value" VARCHAR)[]), alternativeNames STRUCT(fullName STRUCT("value" VARCHAR, evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[]), shortNames STRUCT("value" VARCHAR, evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[])[], ecNumbers STRUCT(evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[], "value" VARCHAR)[])[], allergenName STRUCT(evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[], "value" VARCHAR))[], includes STRUCT(recommendedName STRUCT(fullName STRUCT(evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[], "value" VARCHAR), ecNumbers STRUCT(evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[], "value" VARCHAR)[], shortNames STRUCT("value" VARCHAR, evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[])[]), alternativeNames STRUCT(fullName STRUCT("value" VARCHAR, evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[]), shortNames STRUCT("value" VARCHAR, evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[])[], ecNumbers STRUCT(evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[], "value" VARCHAR)[])[])[], innNames STRUCT("value" VARCHAR, evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[])[], recommendedName STRUCT(fullName STRUCT("value" VARCHAR, evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[]), shortNames STRUCT("value" VARCHAR, evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[])[], ecNumbers STRUCT("value" VARCHAR, evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[])[]), submissionNames STRUCT(fullName STRUCT(evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[], "value" VARCHAR), ecNumbers STRUCT(evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[], "value" VARCHAR)[])[])` | nested | `proteinDescription` | proteinDescription minus flag (kept whole: recommendedName, alternativeNames, submissionNames, contains, includes, ...); protein_name / alt_protein_names / ec_numbers are projections of it. |
| `genes_full` | `list<l: struct<geneName: struct<evidences: list<l: struct<evidenceCode: string, source: string, id: string>>, value: string>, orderedLocusNames: list<l: struct<evidences: list<l: struct<evidenceCode: string, source: string, id: string>>, value: string>>, orfNames: list<l: struct<evidences: list<l: struct<evidenceCode: string, source: string, id: string>>, value: string>>, synonyms: list<l: struct<evidences: list<l: struct<evidenceCode: string, source: string, id: string>>, value: string>>>>` | nested | `genes` | Full genes array (geneName, synonyms, orfNames, orderedLocusNames with evidences); gene_names / gene_synonyms are projections of it. |
| `keywords_full` | `list<l: struct<id: string, category: string, name: string, evidences: list<l: struct<evidenceCode: string, source: string, id: string>>>>` | nested | `keywords` | Full keywords array (id, name, category, evidences); keyword_ids / keyword_names / keyword_categories are projections of it. |
| `organism_hosts` | `STRUCT(scientificName VARCHAR, commonName VARCHAR, taxonId BIGINT, synonyms VARCHAR[])[]` | nested | `organismHosts` | Full nested organismHosts array (for viruses — host organisms). May be null. |
| `gene_locations` | `STRUCT(geneEncodingType VARCHAR, evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[], "value" VARCHAR)[]` | nested | `geneLocations` | Full nested geneLocations array (mitochondrial, plastid, etc.). May be null. |

## `features`

One row per positional annotation (domain, signal, transmembrane, etc). Sorted by (reviewed DESC, taxid ASC, acc ASC) for locality; use ORDER BY start_pos for position-sorted queries within a protein.

- Primary key: ``
- Foreign keys: `acc` → `entries.acc`, `taxid` → `entries.taxid`
- Sort order: `reviewed DESC, taxid ASC, acc ASC`
- Partitioning: hive by `review_status=swissprot|trembl`

| Column | Type | Category | Source path | Description |
| --- | --- | --- | --- | --- |
| `acc` | `string` | convenience | `primaryAccession` | Parent entry's primary accession. Foreign key → entries.acc. |
| `reviewed` | `bool` | convenience | `entryType` | True if the parent entry is Swiss-Prot (reviewed). |
| `taxid` | `int64` | convenience | `organism.taxonId` | NCBI taxonomy ID of the parent entry. Foreign key → entries.taxid. |
| `organism_name` | `string` | convenience | `organism.scientificName` | Scientific name of the parent entry's organism (denormalized for convenience). |
| `seq_length` | `int32` | convenience | `sequence.length` | Sequence length of the parent entry (denormalized for coverage calculations). |
| `type` | `string` | convenience | `features.type` | Feature type (e.g. 'Domain', 'Signal peptide', 'Natural variant'). |
| `start_pos` | `int32` | convenience | `features.location.start.value` | Start position in the sequence (1-based, inclusive). Cast to integer. |
| `end_pos` | `int32` | convenience | `features.location.end.value` | End position in the sequence (1-based, inclusive). Cast to integer. |
| `start_modifier` | `string` | convenience | `features.location.start.modifier` | Position modifier: 'EXACT', 'OUTSIDE', 'UNSURE', or null. |
| `end_modifier` | `string` | convenience | `features.location.end.modifier` | Position modifier: 'EXACT', 'OUTSIDE', 'UNSURE', or null. |
| `description` | `string` | convenience | `features.description` | Free-text description of the feature annotation. |
| `feature_id` | `VARCHAR` | convenience | `features.featureId` | UniProt feature identifier (e.g. PRO_0000001234). May be null. |
| `evidence_codes` | `list<l: string>` | convenience | `features.evidences.evidenceCode` | List of evidence codes (e.g. ECO:0000269) supporting this feature. |
| `location_sequence` | `VARCHAR` | convenience | `features.location.sequence` | Sequence the feature location refers to (isoform or alternative sequence), when not the canonical one. |
| `original_sequence` | `VARCHAR` | convenience | `features.alternativeSequence.originalSequence` | Original amino acids before the variant/mutation. Null for non-variant features. |
| `alternative_sequences` | `VARCHAR[]` | convenience | `features.alternativeSequence.alternativeSequences` | List of alternative amino acid sequences for variant features. |
| `ligand_name` | `VARCHAR` | convenience | `features.ligand.name` | Name of the bound ligand (for Binding site features). May be null. |
| `ligand_id` | `VARCHAR` | convenience | `features.ligand.id` | ChEBI or other identifier for the ligand. May be null. |
| `ligand_label` | `VARCHAR` | convenience | `features.ligand.label` | Label distinguishing multiple ligands in the same entry. May be null. |
| `ligand_note` | `VARCHAR` | convenience | `features.ligand.note` | Additional note about the ligand binding. May be null. |
| `feature_residual` | `STRUCT(alternativeSequence STRUCT(originalSequence VARCHAR, alternativeSequences VARCHAR[]), evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[], featureCrossReferences STRUCT("database" VARCHAR, id VARCHAR)[], ligand STRUCT("name" VARCHAR, id VARCHAR, "label" VARCHAR, note VARCHAR), ligandPart STRUCT("name" VARCHAR, id VARCHAR, note VARCHAR))` | nested | `features` | Feature fields not in the convenience columns: evidences (full), featureCrossReferences, ligand, ligandPart, alternativeSequence. bin/reconstruct.py merges it with the convenience columns. |

## `xrefs`

One row per cross-reference to an external database (PDB, Ensembl, GO, InterPro, etc).

- Primary key: ``
- Foreign keys: `acc` → `entries.acc`, `taxid` → `entries.taxid`
- Sort order: `reviewed DESC, taxid ASC, acc ASC`
- Partitioning: hive by `review_status=swissprot|trembl`

| Column | Type | Category | Source path | Description |
| --- | --- | --- | --- | --- |
| `acc` | `string` | convenience | `primaryAccession` | Parent entry's primary accession. Foreign key → entries.acc. |
| `reviewed` | `bool` | convenience | `entryType` | True if the parent entry is Swiss-Prot (reviewed). |
| `taxid` | `int64` | convenience | `organism.taxonId` | NCBI taxonomy ID of the parent entry. Foreign key → entries.taxid. |
| `database` | `string` | convenience | `uniProtKBCrossReferences.database` | External database name (e.g. 'PDB', 'Ensembl', 'GO', 'InterPro'). |
| `id` | `string` | convenience | `uniProtKBCrossReferences.id` | Identifier in the external database (e.g. '1ABC' for PDB). |
| `properties` | `STRUCT("key" VARCHAR, "value" VARCHAR)[]` | convenience | `uniProtKBCrossReferences.properties` | Database-specific key-value properties as a nested struct/list. |
| `isoform_id` | `VARCHAR` | convenience | `uniProtKBCrossReferences.isoformId` | Isoform accession if this xref is specific to an isoform. May be null. |
| `evidences` | `STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[]` | convenience | `uniProtKBCrossReferences.evidences` | Evidence records supporting this cross-reference. May be null. |

## `comments`

One row per comment annotation (function, subcellular location, disease, etc).

- Primary key: ``
- Foreign keys: `acc` → `entries.acc`, `taxid` → `entries.taxid`
- Sort order: `reviewed DESC, taxid ASC, acc ASC`
- Partitioning: hive by `review_status=swissprot|trembl`

| Column | Type | Category | Source path | Description |
| --- | --- | --- | --- | --- |
| `acc` | `string` | convenience | `primaryAccession` | Parent entry's primary accession. Foreign key → entries.acc. |
| `reviewed` | `bool` | convenience | `entryType` | True if the parent entry is Swiss-Prot (reviewed). |
| `taxid` | `int64` | convenience | `organism.taxonId` | NCBI taxonomy ID of the parent entry. Foreign key → entries.taxid. |
| `comment_type` | `string` | convenience | `comments.commentType` | Comment type (e.g. 'FUNCTION', 'DISEASE', 'SUBCELLULAR LOCATION'). |
| `text_value` | `string` | convenience | `comments.texts.value` | Concatenated free-text values (paragraphs joined by double newline). Null for structured-only comments. |
| `comment` | `string` | nested | `comments` | Full comment as JSON string (lossless). Use comment->>'$.key' to extract fields. |

## `publications`

One row per literature citation or submission (derived from UniProtKB entry.references).

- Primary key: ``
- Foreign keys: `acc` → `entries.acc`, `taxid` → `entries.taxid`
- Sort order: `reviewed DESC, taxid ASC, acc ASC`
- Partitioning: hive by `review_status=swissprot|trembl`

| Column | Type | Category | Source path | Description |
| --- | --- | --- | --- | --- |
| `acc` | `string` | convenience | `primaryAccession` | Parent entry's primary accession. Foreign key → entries.acc. |
| `reviewed` | `bool` | convenience | `entryType` | True if the parent entry is Swiss-Prot (reviewed). |
| `taxid` | `int64` | convenience | `organism.taxonId` | NCBI taxonomy ID of the parent entry. Foreign key → entries.taxid. |
| `reference_number` | `int32` | convenience | `references.referenceNumber` | Position of this reference in the entry's reference list (1-based). |
| `citation_type` | `string` | convenience | `references.citation.citationType` | Citation type: 'journal article', 'submission', 'book', 'patent', etc. |
| `citation_id` | `string` | convenience | `references.citation.id` | Citation identifier (PubMed ID, DOI, or other). May be null for submissions. |
| `title` | `VARCHAR` | convenience | `references.citation.title` | Publication title. May be null for submissions without titles. |
| `authors` | `VARCHAR[]` | convenience | `references.citation.authors` | List of author names. May be null. |
| `authoring_group` | `VARCHAR[]` | convenience | `references.citation.authoringGroup` | Authoring group/consortium name. May be null. |
| `publication_date` | `string` | convenience | `references.citation.publicationDate` | Publication or submission date (string, as provided by UniProt). |
| `journal` | `VARCHAR` | convenience | `references.citation.journal` | Journal name. Null for non-journal citations. |
| `volume` | `VARCHAR` | convenience | `references.citation.volume` | Journal volume. Null for non-journal citations. |
| `first_page` | `VARCHAR` | convenience | `references.citation.firstPage` | First page number. Null for non-journal citations. |
| `last_page` | `VARCHAR` | convenience | `references.citation.lastPage` | Last page number. Null for non-journal citations. |
| `submission_database` | `VARCHAR` | convenience | `references.citation.submissionDatabase` | Database name for submissions (e.g. 'EMBL/GenBank/DDBJ'). Null otherwise. |
| `citation_xrefs` | `STRUCT("database" VARCHAR, id VARCHAR)[]` | convenience | `references.citation.citationCrossReferences` | Cross-references within the citation (PubMed, DOI, etc.). |
| `reference_positions` | `VARCHAR[]` | convenience | `references.referencePositions` | List of reference position strings (e.g. 'NUCLEOTIDE SEQUENCE'). |
| `reference_comments` | `STRUCT("value" VARCHAR, "type" VARCHAR, evidences STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[])[]` | convenience | `references.referenceComments` | List of reference comment structs (scope, source, etc.). |
| `evidences` | `STRUCT(evidenceCode VARCHAR, "source" VARCHAR, id VARCHAR)[]` | convenience | `references.evidences` | Evidence records for this reference. May be null. |
| `reference_residual` | `STRUCT(citation STRUCT(address VARCHAR, bookName VARCHAR, editors VARCHAR[], institute VARCHAR, locator VARCHAR, patentNumber VARCHAR, publisher VARCHAR))` | nested | `references` | Reference fields not in the convenience columns, including citation extras (bookName, editors, publisher, address, institute, patentNumber, locator). bin/reconstruct.py merges it with the convenience columns. |

## `accession_map`

One row per primary or secondary accession, mapping to the current primary accession. The lookup table for point queries and for resolving retired accessions.

- Primary key: `acc, primary_acc`
- Foreign keys: `primary_acc` → `entries.acc`
- Sort order: `reviewed DESC, acc ASC, primary_acc ASC`
- Partitioning: hive by `review_status=swissprot|trembl`

| Column | Type | Category | Source path | Description |
| --- | --- | --- | --- | --- |
| `acc` | `string` | convenience | `primaryAccession | secondaryAccessions` | Primary or secondary accession (the lookup key). A secondary accession can map to more than one primary (split entries). |
| `primary_acc` | `string` | convenience | `primaryAccession` | Current primary accession the key resolves to (FK → entries.acc). |
| `is_primary` | `bool` | convenience | derived | True when acc == primary_acc. |
| `reviewed` | `bool` | convenience | `entryType` | True if the primary entry is Swiss-Prot (copied from entries.reviewed). |
| `taxid` | `int64` | convenience | `organism.taxonId` | NCBI taxonomy ID of the primary entry (copied from entries.taxid). |

## Versioning policy

The schema version (`schema_version` in `manifest.json`, `version` in `datapackage.json`, and the `schema_version` key in every Parquet footer) is a semver that is independent of the UniProt data release; a data release normally ships with an unchanged schema.

- **Major:** any column renamed, removed, retyped or reordered; any table renamed or removed; partition or sort-order change; a change in NULL/empty-list convention.
- **Minor:** a column or table added; an enum gaining a value; a new manifest key.
- **Patch:** a bug fix that changes values without changing the schema, compression or file-size changes, documentation.
- The UniProt data release (`2026_03`) and the schema version are independent; a data release normally ships with an unchanged schema.

`manifest.json` also carries an integer `version`: the manifest *format* version, bumped only when the manifest's own structure changes.
